# 原型生成说明

生成方式：内置 image_gen 图片编辑工具。只制作产品原型，没有使用 CLI/API 回退，也没有修改业务页面。

两张图片均基于本轮前置原型继续编辑，保留品类、日期、时长、数据区间筛选，并增加保存及复用组合。

以下是两张最终交付图实际使用的完整编辑提示词：

## 1. 品类收起 · 常用筛选复用

```text
Use case: precise-object-edit, a small addition to the existing CreatiSignal prototype. Add a SAVE AND REUSE FILTER COMBINATIONS feature. Preserve the complete existing visual style, sidebar, category state, keyword 猫窝, platform TikTok, country 美国, 投放中, 最近14天, 播放量 sort, ≤15秒 and 15–30秒 selected, all three data filters 点赞数/消耗（USD）/触达受众, result count, photos and card metrics. Never add back the deleted page subtitle, helper sentence under time pills, 示例数据, or the small badge under ≤15秒. Do not create a separate page, dashboard, overlay dialog or promotional banner. Largest clear image resolution supported, one flat desktop UI screenshot, sharp accurate Chinese.
Within the EXISTING 数据筛选 row there is wide empty space on the RIGHT, after the three range dropdowns. Add two compact, right-aligned buttons: a white outlined 常用筛选 with chevron, then a pale-lime 保存当前筛选 button with a small bookmark/save outline icon. These save ALL filters, not only the data filters, but do not add explanatory copy into the image. Keep both buttons in the existing row without adding another toolbar row or colliding with 触达受众. Keep 清除筛选 and 恢复默认 below in the results row. All original controls remain available. THIS STATE: categories remain collapsed into one horizontal row, 展开 at far right. CLOSE the open 点赞数 menu, field still 全部. Open ONLY the new 常用筛选 dropdown, anchored beneath its button and aligned inside the right edge of the content. Natural white rounded popover about 280 logical pixels wide with fine border and subtle shadow. Show exactly three saved combination names as single-click rows: 美国猫窝 · 近14天 (green checkmark selected), 宠物用品 · 高点赞, 美国 · 低消耗素材. Each row has a discreet three-dot menu icon at its far right for rename/delete actions; do not render further submenus. No additional text or footer, no 保存成功 toast, no modal. Keep the nearby 保存当前筛选 button visible. Preserve all other rows and the photo grid.
```

## 2. 品类展开 · 消耗区间筛选

```text
Use case: precise-object-edit, a small addition to the existing CreatiSignal prototype. Add a SAVE AND REUSE FILTER COMBINATIONS feature. Preserve the complete existing visual style, sidebar, category state, keyword 猫窝, platform TikTok, country 美国, 投放中, 最近14天, 播放量 sort, ≤15秒 and 15–30秒 selected, all three data filters 点赞数/消耗（USD）/触达受众, result count, photos and card metrics. Never add back the deleted page subtitle, helper sentence under time pills, 示例数据, or the small badge under ≤15秒. Do not create a separate page, dashboard, overlay dialog or promotional banner. Largest clear image resolution supported, one flat desktop UI screenshot, sharp accurate Chinese.
Within the EXISTING 数据筛选 row there is wide empty space on the RIGHT, after the three range dropdowns. Add two compact, right-aligned buttons: a white outlined 常用筛选 with chevron, then a pale-lime 保存当前筛选 button with a small bookmark/save outline icon. These save ALL filters, not only the data filters, but do not add explanatory copy into the image. Keep both buttons in the existing row without adding another toolbar row or colliding with 触达受众. Keep 清除筛选 and 恢复默认 below in the results row. All original controls remain available. THIS STATE: categories remain EXPANDED into the same three inline rows with 收起 at far right, exactly as the input. Keep the existing 消耗（USD） menu OPEN and its exact entries 全部 checked, <$100, $100–<$500, $500–<$1,000, $1,000–<$5,000, ≥$5,000. Both new 常用筛选 and 保存当前筛选 buttons remain visible but CLOSED, so the spend menu is the only open popover. Keep the exact original category labels, original wrapping and remaining card layout. Add only those two save/reuse controls.
```

